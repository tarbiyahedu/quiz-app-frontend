// Common utility functions used throughout the app

// Classnames utility
export function cn(...args: any[]) {
  return args.filter(Boolean).join(' ');
}

// Add any other utility functions here as needed by your components 